package topos.pruebas;

public class PruebaElemento {
	
	/**public static void main(String[] args) {
		
		PanelBasico panel1 = new PanelBasico(0,0);
		Elemento elem = new Elemento();
		panel1.introducirElemento(elem);
		System.out.println(elem.getPanel());
		for (Elemento ele : panel1.getElementos()) {
			System.out.println(ele.getPanel().getPosicion());
		}
		// El panel coincide en ambos casos
		
		panel1.quitarElemento(elem);
		System.out.println(elem.getPanel());
		for (Elemento ele : panel1.getElementos()) {
			System.out.println(ele.getPanel().getPosicion());
		}
		// Como vemos, la lista está vacía
	}**/
}
